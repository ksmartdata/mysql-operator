# chainsaw --values file: MySQL 5.7.44 (equivalence class Major == 5, the
# only 5.7 version in production)
version: "5.7.44"
image: docker.io/library/mysql:5.7.44
# golden path relative to each test directory (tests/<case>/)
golden: ../../golden/my.cnf-5.7.44.cnf
