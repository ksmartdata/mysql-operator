# StatefulSet 双副本 ready（纯字段断言）。
# CR 的 Ready condition 用 kubectl wait 脚本步骤检查（chainsaw 的 JMESPath
# 投影断言在本 CRD 上不可靠，首轮 CI 实测 "field not found"），不在此文件断言。
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: e2e-mysql
status:
  replicas: 2
  readyReplicas: 2
